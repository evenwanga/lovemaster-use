# 《珍爱到家·商家端》产品需求文档 (PRD)

| 文档属性 | 内容 |
| :--- | :--- |
| **项目名称** | 珍爱到家·商家端 (Love Delivered Merchant) |
| **文档版本** | V1.0 |
| **产品定位** | 婚恋红娘的一站式经营工作台 (SaaS) |
| **核心理念** | 像管理电商后台一样管理婚恋服务 |
| **对应客户端** | 珍爱到家 (用户端) |

---

## 1. 产品愿景
打造婚恋行业的“美团商家版”。通过移动端SaaS工具，帮助红娘/婚介机构高效管理单身资源、快速响应用户咨询、标准化履约线下约会服务，并实现经营数据可视化。

---

## 2. 核心业务流程
1.  **资源上架**：商户录入嘉宾信息 -> 自动同步至C端店铺。
2.  **售前咨询**：接收C端咨询 -> IM沟通 -> 发送嘉宾推荐卡片 -> 引导下单。
3.  **服务履约**：查看订单 -> **驱动服务节点** (点击“确认排期”、“确认见面”) -> C端同步更新进度条。
4.  **资金结算**：订单完成 -> 资金解冻 -> 提现。

---

## 3. 功能模块详解

### 3.1 工作台 (Dashboard) —— 首页
**目标**：经营数据概览与待办事项驱动。

*   **数据看板**：
    *   **今日数据**：新客咨询数、今日订单、今日收入。
    *   **核心指标**：店铺评分、响应时长、7日曝光量。
*   **待办事项 (Todo List)**：
    *   待回复消息 (高亮)。
    *   待履约订单 (提示：有3个约会等待安排见面)。
    *   待审核资源。
*   **快捷入口**：录入嘉宾、扫码核销、分享店铺。

### 3.2 消息 (IM System) —— 核心交互
**目标**：极速响应，线索转化。

*   **会话列表**：按“未读”、“意向高低”排序。显示用户最后一条消息摘要。
*   **聊天窗口**：
    *   **用户画像**：侧边栏/顶部显示当前聊天的用户基础画像（如有）。
    *   **意向捕捉**：如果用户是从某位嘉宾详情页进来的，顶部悬浮显示“用户正在浏览：[嘉宾姓名]”。
    *   **快捷工具**：
        *   **发送推荐**：从资源库选择嘉宾，发送卡片（包含照片、资料）。
        *   **快捷短语**：常用话术一键发送。
        *   **发送服务**：推送服务套餐购买链接。

### 3.3 资源管理 (Resources) —— 货架管理
**目标**：管理 SKU（单身嘉宾）。

*   **嘉宾列表**：
    *   状态筛选：上架中、已脱单（下架）、审核中。
    *   操作：编辑、下架、分享海报。
*   **录入/编辑嘉宾**：
    *   **基础信息**：头像、身高、学历、职业、薪资（必填）。
    *   **相册管理**：上传生活照。
    *   **红娘寄语**：填写推荐理由（显示在C端详情页）。
    *   **私密设置**：设置是否允许C端直接查看照片（或需申请查看）。

### 3.4 订单管理 (Order Fulfillment) —— 履约核心
**目标**：标准化服务流程，驱动状态机。

*   **订单列表**：
    *   Tabs：全部、服务中、待评价、已完成。
*   **订单详情 & 履约操作**：
    *   展示购买的套餐信息（如：季度会员，剩余次数 5/6）。
    *   **服务权益记录 (Service Records)**：
        *   针对每一条具体的“约会服务”，商户拥有**写权限**。
        *   **状态推进操作**：
            1.  **初始状态**：点击【开始策划】 -> 状态变更为 `Planning`。
            2.  **策划阶段**：点击【确认约会信息】（输入时间、地点、对象） -> 状态变更为 `Confirmed` (C端显示待见面)。
            3.  **见面后**：点击【完成服务】 -> 状态变更为 `Completed` (C端显示待评价)。
            4.  **异常处理**：点击【取消/流局】 -> 恢复权益次数。

### 3.5 店铺/我的 (Store Profile)
*   **店铺设置**：修改头像、公告、营业时间、地址。
*   **资质中心**：查看保证金状态、实名认证状态、企业执照。
*   **评价管理**：回复C端用户的评价。
*   **钱包**：余额提现、收支明细。

---

## 4. 数据结构扩展 (B-side Specific)

### 4.1 商家特有字段
*   `income_balance`: 钱包余额。
*   `pending_tasks`: 待办任务数。
*   `visit_stats`: 访问统计数据。

### 4.2 交互逻辑差异
*   **C端**：只读服务进度。
*   **B端**：**写入**服务进度 (Write Access to ServiceRecord)。

---

## 5. UI 设计风格
*   **风格**：这也是“美团/外卖商家版”风格。
*   **色调**：
    *   主色：保留品牌黄 (`#FFC300`)，但增加更多的**蓝色** (`#1677FF`) 用于理性的管理操作界面。
    *   背景：白色为主，强调信息密度。
*   **布局**：底部导航 (工作台、消息、资源、订单、我的)。
